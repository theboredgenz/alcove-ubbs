# UBBS V2 — Python runtime dependencies
# Use: pip install -r requirements.txt
#
# For development extras (pytest, ruff, mypy, hypothesis), use:
#   pip install -e ".[dev]"
streamlit>=1.38.0
streamlit-cookies-controller>=0.0.4
supabase>=2.7.0
pandas>=2.2.0
python-dotenv>=1.0.0
